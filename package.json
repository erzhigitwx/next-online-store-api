{
  "name": "server",
  "version": "1.0.0",
  "description": "json server",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "json-server": "json-server --watch db.json --port ${PORT:-5000}"
  },
  "author": "erzhigit",
  "license": "ISC",
  "dependencies": {
    "json-server": "^0.17.3"
  }
}
